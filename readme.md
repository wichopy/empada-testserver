# Websocket Express Server for Empada


`npm install sequelize-cli -g` to run migrations and seeds.
`npm install pg -g` for migrations and seeds.

`npm install` to get dependencies.

`npm start` to start server
